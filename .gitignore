# Secrets — never commit (the local .env holds real API keys)
.env
.env.*
!.env.example

# Databases / caches
*.db
*.sqlite
*.sqlite3
# ...but DO commit the app's database so runs / the leaderboard persist across
# instances (see "Data persistence" in the README). The test DB stays ignored.
!nichefit.db

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/
env/

# Tooling / OS
node_modules/
.DS_Store
.idea/
.vscode/

# Claude Code local tooling (machine-specific)
.claude/
